"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Register(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[role,setRole]=useState("CONSUMER");const[org,setOrg]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/v1/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({fullName:name,email,password,role,organizationName:role==="MANUFACTURER"?org:undefined})});const d=await r.json();if(!r.ok){setError(d.error||"Registration failed");return}localStorage.setItem("authenti_token",d.accessToken);router.push(role==="MANUFACTURER"?"/dashboard":"/")}return <main className="page"><section className="hero auth"><div className="badge">AUTHENTICHECK • REGISTER</div><h1>Create account.</h1><form onSubmit={submit}><input placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/><select value={role} onChange={e=>setRole(e.target.value)}><option value="CONSUMER">Consumer</option><option value="MANUFACTURER">Manufacturer</option></select>{role==="MANUFACTURER"&&<input placeholder="Organization name" value={org} onChange={e=>setOrg(e.target.value)} required/>}<button>Create account</button>{error&&<p>{error}</p>}</form></section></main>}